import { PageTopGradient } from "@/components/page-top-gradient"

/** Compendium routes use the taller top fade (see app/compendium/layout.tsx). */
export function CompendiumTopGradient() {
  return <PageTopGradient size="full" />
}
